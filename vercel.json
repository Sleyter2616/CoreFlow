{
  "version": 2,
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && npm run seed:prod && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXTAUTH_URL": "https://${VERCEL_URL}",
    "POSTGRES_PRISMA_URL": "${POSTGRES_PRISMA_URL}"
  }
}
