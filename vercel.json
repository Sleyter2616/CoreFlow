{
  "version": 2,
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx ts-node scripts/seed-prod.ts && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXTAUTH_URL": "https://${VERCEL_URL}",
    "POSTGRES_PRISMA_URL": "${POSTGRES_PRISMA_URL}"
  }
}
